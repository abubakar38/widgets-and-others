import 'package:flutter/material.dart';

class Mytxt extends StatelessWidget {
  const Mytxt({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text("this is abubakar"),
        ),
      ),
    );
  }
}
